Download packages:
	
Connect:
	virt-viewer	--connect <url> --attach
	<url> get from virt-manager > view > connection details > libvirt URI 

virsh net-start default
virt-viewer --connect qemu:///system --attach &
